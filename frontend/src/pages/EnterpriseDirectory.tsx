import { useEffect, useMemo, useState } from 'react';
import { BadgeCheck, Building2, Globe2, Inbox, Leaf, List, Map, MapPin, RefreshCw, Search, SlidersHorizontal, X } from 'lucide-react';
import { useNavigate, useSearchParams } from 'react-router-dom';
import { QuickFilterChips, SearchPagination } from '@/src/components/SearchControls';
import { cn } from '@/src/lib/utils';
import { DIRECTORY_INDUSTRIES, DIRECTORY_PROVINCES } from '@/src/lib/enterpriseDirectoryFilters';
import { EMPTY_SEARCH_QUICK_FILTERS, hasActiveQuickFilters, readQuickFilters, type SearchQuickFilters, writeQuickFilters } from '@/src/lib/searchFilters';
import { api, type EnterpriseDirectoryItem } from '@/src/services/api';
import { useAuth } from '@/src/context/AuthContext';

const PAGE_SIZE = 24;
const initialFilters = { province: '', city: '', industry: '', q: '', tech_keyword: '', min_credit: '', min_capacity: '', max_capacity: '', capacity_status: '', business_status: '', registered_capital: '', quickFilters: { ...EMPTY_SEARCH_QUICK_FILTERS } };
type DirectoryFilters = typeof initialFilters;

function readFilters(params: URLSearchParams): DirectoryFilters {
  return { province: params.get('province') || '', city: params.get('city') || '', industry: params.get('industry') || '', q: params.get('q') || '', tech_keyword: params.get('tech_keyword') || '', min_credit: params.get('min_credit') || '', min_capacity: params.get('min_capacity') || '', max_capacity: params.get('max_capacity') || '', capacity_status: params.get('capacity_status') || '', business_status: params.get('business_status') || '', registered_capital: params.get('min_registered_capital') || '', quickFilters: readQuickFilters(params) };
}

function writeFilters(filters: DirectoryFilters, page = 1, sort = 'credit') {
  const params = new URLSearchParams();
  Object.entries(filters).forEach(([key, value]) => { if (key !== 'quickFilters' && value) params.set(key, String(value)); });
  writeQuickFilters(params, filters.quickFilters);
  if (sort !== 'credit') params.set('sort', sort);
  if (page > 1) params.set('page', String(page));
  return params;
}

export default function EnterpriseDirectory() {
  const [params, setParams] = useSearchParams();
  const navigate = useNavigate();
  const { requestLogin } = useAuth();
  const [filters, setFilters] = useState<DirectoryFilters>(() => readFilters(params));
  const [rows, setRows] = useState<EnterpriseDirectoryItem[]>([]);
  const [total, setTotal] = useState(0);
  const [pages, setPages] = useState(0);
  const [view, setView] = useState<'list' | 'map'>('list');
  const [advanced, setAdvanced] = useState(false);
  const [mobileFiltersOpen, setMobileFiltersOpen] = useState(false);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState('');
  const [retryTick, setRetryTick] = useState(0);
  const searchKey = params.toString();
  const applied = useMemo(() => readFilters(new URLSearchParams(searchKey)), [searchKey]);
  const page = Math.max(1, Number(params.get('page') || 1));
  const sort = params.get('sort') || 'credit';

  useEffect(() => { setFilters(applied); }, [applied]);
  useEffect(() => { let cancelled = false; setLoading(true); setError(''); void api.fetchEnterpriseDirectory({ province: applied.province, city: applied.city, industry: applied.industry, q: applied.q || undefined, tech_keyword: applied.tech_keyword || undefined, min_credit: applied.min_credit ? Number(applied.min_credit) : undefined, min_capacity: applied.min_capacity ? Number(applied.min_capacity) : undefined, max_capacity: applied.max_capacity ? Number(applied.max_capacity) : undefined, capacity_status: applied.capacity_status || undefined, business_status: applied.business_status || undefined, registered_capital: applied.registered_capital ? Number(applied.registered_capital) : undefined, is_export: applied.quickFilters.is_export, has_decision_maker: applied.quickFilters.has_decision_maker, is_little_giant: applied.quickFilters.is_little_giant, is_green_factory: applied.quickFilters.is_green_factory, page, per_page: PAGE_SIZE, sort }).then(response => { if (!cancelled) { setRows(response.enterprises || []); setTotal(response.total || 0); setPages(response.pages || 0); } }).catch(err => { if (!cancelled) { setRows([]); setTotal(0); setPages(0); setError(err instanceof Error ? err.message : '名录加载失败，请稍后重试'); } }).finally(() => { if (!cancelled) setLoading(false); }); return () => { cancelled = true; }; }, [applied, page, retryTick, searchKey, sort]);

  const setFilter = <K extends keyof DirectoryFilters>(key: K, value: DirectoryFilters[K]) => setFilters(previous => ({ ...previous, [key]: value }));
  const updateUrl = (next: DirectoryFilters, nextPage = 1, nextSort = sort) => setParams(writeFilters(next, nextPage, nextSort));
  const apply = () => { updateUrl(filters); setMobileFiltersOpen(false); };
  const toggleQuickFilter = (key: keyof SearchQuickFilters) => { const next = { ...filters, quickFilters: { ...filters.quickFilters, [key]: !filters.quickFilters[key] } }; setFilters(next); updateUrl(next); };
  const clear = () => { const next = { ...initialFilters, quickFilters: { ...EMPTY_SEARCH_QUICK_FILTERS } }; setFilters(next); updateUrl(next); };
  const activeFilterCount = Object.entries(applied).filter(([key, value]) => key !== 'quickFilters' && Boolean(value)).length + (hasActiveQuickFilters(applied.quickFilters) ? 1 : 0);
  const draftFilterCount = Object.entries(filters).filter(([key, value]) => key !== 'quickFilters' && Boolean(value)).length + (hasActiveQuickFilters(filters.quickFilters) ? 1 : 0);

  return <div className="mx-auto flex w-full max-w-[1440px] flex-col gap-5"><section className="panel p-5 md:p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="eyebrow text-brand">Enterprise directory</p><h1 className="mt-1 text-2xl font-black text-ink">名录筛选</h1><p className="mt-2 text-xs text-ink-muted">按地域、行业、技术、产能和可信标签查找合作企业</p></div><div className="flex items-center gap-2"><button type="button" onClick={() => setView('list')} className={cn('btn-secondary btn-sm gap-1.5', view === 'list' && 'border-brand bg-brand-soft text-brand')}><List className="h-3.5 w-3.5" />列表</button><button type="button" onClick={() => setView('map')} className={cn('btn-secondary btn-sm gap-1.5', view === 'map' && 'border-brand bg-brand-soft text-brand')}><Map className="h-3.5 w-3.5" />地图</button><button type="button" onClick={() => setRetryTick(value => value + 1)} className="btn-secondary btn-sm" title="刷新"><RefreshCw className="h-3.5 w-3.5" /></button></div></div><div className="mt-5 flex flex-col gap-3 md:flex-row"><div className="relative min-w-0 flex-1"><Search className="absolute left-3.5 top-1/2 h-4 w-4 -translate-y-1/2 text-brand" /><input data-testid="directory-search-input" value={filters.q} onChange={event => setFilter('q', event.target.value)} onKeyDown={event => { if (event.key === 'Enter') { event.preventDefault(); apply(); } }} placeholder="输入企业名、经营范围或技术关键词" className="input h-11 w-full pl-10" /></div><button type="button" onClick={apply} className="btn-primary h-11 px-6"><Search className="h-4 w-4" />搜索企业</button></div><div className="mt-4"><QuickFilterChips filters={filters.quickFilters} onToggle={toggleQuickFilter} compact /></div></section><div className="grid gap-5 lg:grid-cols-[260px_1fr]"><>{mobileFiltersOpen && <button type="button" aria-label="关闭筛选" onClick={() => setMobileFiltersOpen(false)} className="fixed inset-0 z-40 bg-slate-900/25 lg:hidden" />}</><aside className={cn('panel h-fit p-4 lg:sticky lg:top-24', mobileFiltersOpen ? 'fixed inset-y-0 left-0 z-50 block w-[min(88vw,320px)] overflow-y-auto rounded-none border-r' : 'hidden lg:block')}><div className="flex items-center justify-between"><div className="flex items-center gap-2"><SlidersHorizontal className="h-4 w-4 text-brand" /><h2 className="text-sm font-bold text-ink">筛选条件</h2></div><div className="flex items-center gap-3"><button type="button" onClick={clear} disabled={!draftFilterCount} className="text-[11px] font-bold text-brand disabled:text-ink-faint">清空</button><button type="button" onClick={() => setMobileFiltersOpen(false)} aria-label="关闭筛选" className="text-ink-muted lg:hidden"><X className="h-4 w-4" /></button></div></div><div className="mt-4 space-y-4"><label className="block"><span className="field-label">省份</span><select value={filters.province} onChange={event => setFilter('province', event.target.value)} className="input w-full text-xs"><option value="">全部省份</option>{DIRECTORY_PROVINCES.map(item => <option key={item.key} value={item.key}>{item.label}</option>)}</select></label><label className="block"><span className="field-label">城市</span><input value={filters.city} onChange={event => setFilter('city', event.target.value)} placeholder="如：佛山市" className="input w-full text-xs" /></label><label className="block"><span className="field-label">行业</span><select value={filters.industry} onChange={event => setFilter('industry', event.target.value)} className="input w-full text-xs"><option value="">全部行业</option>{DIRECTORY_INDUSTRIES.map(item => <option key={item.key} value={item.key}>{item.label}</option>)}</select></label><button type="button" onClick={() => setAdvanced(value => !value)} className="w-full rounded-md border border-dashed border-border py-2 text-xs font-bold text-ink-muted hover:bg-surface-subtle">{advanced ? '收起高级筛选' : '展开高级筛选'}</button>{advanced && <div className="space-y-4 border-t border-border pt-4"><label className="block"><span className="field-label">技术关键词</span><input value={filters.tech_keyword} onChange={event => setFilter('tech_keyword', event.target.value)} placeholder="如：精密注塑" className="input w-full text-xs" /></label><label className="block"><span className="field-label">最低信用分</span><select value={filters.min_credit} onChange={event => setFilter('min_credit', event.target.value)} className="input w-full text-xs"><option value="">不限</option><option value="60">≥ 60</option><option value="70">≥ 70</option><option value="80">≥ 80</option><option value="90">≥ 90</option></select></label><div className="grid grid-cols-2 gap-2"><input type="number" min="0" value={filters.min_capacity} onChange={event => setFilter('min_capacity', event.target.value)} placeholder="最低产能" className="input text-xs" /><input type="number" min="0" value={filters.max_capacity} onChange={event => setFilter('max_capacity', event.target.value)} placeholder="最高产能" className="input text-xs" /></div><select value={filters.capacity_status} onChange={event => setFilter('capacity_status', event.target.value)} className="input w-full text-xs"><option value="">产能状态不限</option><option value="ample">当前有余量</option><option value="tight">排期较紧</option></select><select value={filters.business_status} onChange={event => setFilter('business_status', event.target.value)} className="input w-full text-xs"><option value="">经营状态不限</option><option value="存续">存续</option><option value="在业">在业</option><option value="注销">注销</option></select><input type="number" min="0" value={filters.registered_capital} onChange={event => setFilter('registered_capital', event.target.value)} placeholder="最低注册资本（万元）" className="input w-full text-xs" /></div>}<button type="button" onClick={apply} className="btn-secondary w-full">应用筛选</button></div></aside><section className="min-w-0"><div className="mb-3 flex flex-wrap items-center justify-between gap-3"><div><p className="text-sm text-ink-muted">找到 <strong className="text-ink">{total}</strong> 家企业{pages ? ` · 第 ${page} / ${pages} 页` : ''}</p>{activeFilterCount > 0 && <p className="mt-1 text-xs text-ink-faint">已启用 {activeFilterCount} 项筛选</p>}</div><div className="flex items-center gap-2"><button type="button" onClick={() => setMobileFiltersOpen(true)} className="btn-secondary btn-sm lg:hidden"><SlidersHorizontal className="h-3.5 w-3.5" />筛选{activeFilterCount ? ` (${activeFilterCount})` : ''}</button><select aria-label="企业排序" value={sort} onChange={event => updateUrl(applied, 1, event.target.value)} className="input h-9 text-xs"><option value="credit">信用优先</option><option value="updated">最近更新</option><option value="capacity">产能优先</option><option value="name">名称排序</option></select></div></div>{error && <div className="mb-3 flex items-center justify-between gap-3 rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700"><span>{error}</span><button type="button" onClick={() => setRetryTick(value => value + 1)} className="font-bold">重试</button></div>}{view === 'map' ? <div className="panel min-h-[520px] bg-surface-subtle p-5"><div className="grid gap-3 md:grid-cols-2">{rows.filter(row => row.latitude != null && row.longitude != null).map(row => <button key={row.id} type="button" onClick={() => navigate(`/factory/${row.id}`)} className="flex items-start gap-3 rounded-md border border-border bg-white p-3 text-left hover:border-brand/40"><MapPin className="mt-0.5 h-4 w-4 shrink-0 text-brand" /><span><strong className="block text-xs text-ink">{row.name}</strong><span className="mt-1 block text-[11px] text-ink-muted">{row.province} {row.city} · {row.latitude?.toFixed(4)}, {row.longitude?.toFixed(4)}</span></span></button>)}</div>{!rows.some(row => row.latitude != null && row.longitude != null) && <div className="flex min-h-[420px] items-center justify-center text-center text-xs text-ink-muted">当前结果没有可用经纬度，无法绘制地图；企业仍可在列表模式查看。</div>}</div> : loading ? <div className="panel flex min-h-[420px] items-center justify-center gap-2 text-sm text-ink-muted"><RefreshCw className="h-4 w-4 animate-spin" />正在加载企业名录…</div> : rows.length ? <div className="grid gap-3 md:grid-cols-2">{rows.map(row => <article data-testid="directory-result" key={row.id} className="card-hover p-5"><div className="flex items-start gap-3"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-md bg-brand-soft text-brand"><Building2 className="h-5 w-5" /></span><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-2"><h3 className="truncate text-sm font-bold text-ink" title={row.name}>{row.name}</h3><span className="shrink-0 rounded bg-brand-soft px-1.5 py-1 text-[10px] font-bold text-brand">信用 {Math.round(row.credit_score)}</span></div><p className="mt-1 flex items-center gap-1 text-[11px] text-ink-muted"><MapPin className="h-3 w-3" />{[row.province, row.city].filter(Boolean).join(' ') || '位置待补充'} · {row.industry_code || '行业待补充'}</p><p className="mt-3 line-clamp-2 text-xs leading-5 text-ink-muted">{row.business_scope || '暂无经营范围描述'}</p><div className="mt-3 flex flex-wrap gap-1.5">{row.is_export && <span className="badge badge-success"><Globe2 className="mr-1 inline h-3 w-3" />可出口</span>}{row.has_decision_maker && <span className="badge badge-default">有决策人</span>}{row.is_little_giant && <span className="badge badge-default"><BadgeCheck className="mr-1 inline h-3 w-3" />专精特新</span>}{row.is_green_factory && <span className="badge badge-success"><Leaf className="mr-1 inline h-3 w-3" />绿色工厂</span>}{row.capacity_status && <span className="badge badge-default">{row.capacity_status === 'ample' ? '产能有余量' : '排期较紧'}</span>}{row.registered_capital ? <span className="badge badge-default">注册资本 {row.registered_capital.toLocaleString('zh-CN')} 万</span> : null}</div><p className="mt-3 text-[10px] text-ink-faint">来源：{row.source || '企业档案'} · 更新于 {row.updated_at?.slice(0, 10) || '待更新'}{row.is_demo ? ' · 演示数据' : ''}</p></div></div><div className="mt-4 flex justify-end gap-2"><button type="button" onClick={() => navigate(`/factory/${row.id}`)} className="btn-secondary btn-sm">查看详情</button><button type="button" onClick={() => requestLogin(`/matching?query=${encodeURIComponent(row.name)}&supplier_id=${row.id}`)} className="btn-primary btn-sm">发起询价</button></div></article>)}</div> : <div className="panel flex min-h-[420px] flex-col items-center justify-center text-center"><Inbox className="h-8 w-8 text-ink-faint" /><p className="mt-3 text-sm font-bold text-ink">未找到符合条件的企业</p><p className="mt-1 text-xs text-ink-muted">尝试减少筛选条件或更换关键词。</p></div>}{!loading && <SearchPagination page={page} pages={pages} onChange={nextPage => updateUrl(applied, nextPage)} />}</section></div></div>;
}
